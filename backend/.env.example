# Backend
PORT=4000

# Pakasir — leave BASE_URL pointing at the mock until real sandbox creds arrive,
# then set https://app.pakasir.com + real project slug & api key.
PAKASIR_BASE_URL=http://localhost:4990
PAKASIR_PROJECT=demo
PAKASIR_API_KEY=dev-key

# Reclaim zkFetch — register (free) at https://dev.reclaimprotocol.org
RECLAIM_APP_ID=
RECLAIM_APP_SECRET=

# Stellar
ESCROW_CONTRACT_ID=
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
